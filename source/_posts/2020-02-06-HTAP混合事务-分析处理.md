---
title: HTAP混合事务-分析处理
date: 2020-02-06
comments: false
categories: 读书笔记
---

## OLAP vs. OLTP

* OLTP: 面向应用\最新\细节\简单事务\上千用户\MB-GB\ER模型\行式存储

* OLAP: 面向主题\历史\聚合\历史查询\百万用户\GB-TB\星型模型\列式存储

目前方案折中：OLTP + Snapshot -> OLAP，例如
* Linux系统快照，快照不能太过落后于OLTP
* HANA的增量合并热数据

市场产品：
* 腾讯云TiDB
* 阿里HybridDB for MySQL
* 百度BaikaDB
* MemQL

## 阿里HybridDB for MySQL

1. 2013: 单机中间件
* CPU、内存、锁、日志、事务等竞争
2. 2014：分库分表（类MPP）
* 事务状态机：两阶段提交保证强一致性
* 请求连接池：分级，服务器端共享
* 流式执行：简化支持特定算子
3. 2015：存储优化
* FLASHCACHE -> BCACHE(SSD & SATA)
* SQL优化：InnoDB -> TokuDB，根插入，异步合并到叶子结点
* BinLog流式传输
4. 2016：优化计算支持OLAP
* SQL兼容性：执行计划优化（RBO，CBO）
* 计算引擎：OLAP用Flink，OLTP用MPP
* 数据一致性：MySQL暴露读视图，保障OLAP看到最新数据
* SLA：Query级别做IOPS隔离，是AP和TP单独保障
5. 2017：行列存储优化
* 列存储索引，如位图索引，但AP视图有延迟
6. 2018：共享存储PolarDB，而PolarDB借鉴Amazon Aurora
* 存储和计算分离架构
* BinLog -> Readlog

## BitMap Index vs. B-Tree Index

### 位图索引：每列的散列值各对应一个Bit
优势：
* 硬件加速，按位操作
* 多列聚合 -> 按位逻辑

劣势：
* 高基数列索引占用空间大，查询性能差
* 更新代价大，如果更新散列值，要获取写锁，更新很多索引
* 适用只读，更新少，或追加数据上

场景：
* 数仓、决策系统、OLAP、图数据库

### B-Tree索引：N叉平衡树，每列散列值对应一个RowID
优势：
* 更新效率高

劣势：
* 低基数时索引空间冗余
* 树结构会随记录增多膨胀
* 单一索引路径选择问题

场景：
* 事务型OLTP

## PolarDB vs. Aurora vs. Taurus
TODO